{% extends 'admin.html.twig' %}

{% block content_title %}{{ title }}{% endblock %}

{% block bodyAdmin %}
    <h1>{{ title }}</h1>
    <form>
        <div id="formBuilder" data-url="{{ asset('build/formbuilder-lang') }}"></div>
        <a href="#" id="SaveFormBuilder">Save</a>
    </form>
{% endblock %}

